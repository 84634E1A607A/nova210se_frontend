/**
 * @description Values in this file should correspond to `id` of urls (routers) in `index.tsx`.
 */

export const loginRouterUrl = /^\/login(\/)?$/;

export const chatsRouterUrl = /^\/(.+)chats(\/)?$/;

export const chat_mainRouterUrl = /^\/(.+)\/chats\/([0-9]+)(\/)?$/;

export const chat_detailRouterUrl = /^\/(.+)\/chats\/([0-9]+)\/more(\/)?$/;

export const invitationsRouterUrl = /^\/(.+)\/invitation_list(\/)?$/;
